def test_input_text(expected_result, actual_result):
    assert (expected_result == actual_result), f"expected {expected_result}, got {actual_result}"

#задание реально в одну строку, непонятно как так и почему это считаается правильным,
#уже пыталась завести переменные и использовать ифы, а всё оказалось СИЛЬНО проще


# в общем смысл F-string такой, что f"тут какой-то текст любой который выведется"
# и параметры сюда же пихнуть можно

